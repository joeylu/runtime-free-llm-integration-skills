interface:
  display_name: "Gemini Direct Models"
  short_description: "Wire Gemini stable Interactions and explicit compatibility routes."
  default_prompt: "Use $skill-llm-gemini to build a direct Gemini Developer API integration. Prefer Interactions v1, keep v1beta and generateContent explicit, and resolve exact model, profile, surface/version, URL, role/history, capability, state, tools, pricing, transport, errors, progress, and evidence."

policy:
  allow_implicit_invocation: true
