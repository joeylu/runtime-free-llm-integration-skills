# OpenAI Music Transport

This skill has no documented OpenAI music or audio-generation row.

Do not route speech, transcription, realtime audio, or an invented music model through `RequestKind = music`. A future integration requires an explicit shared request-kind decision, documented catalog row, endpoint, pricing rows, capability rows, and transport contract.

Fail before implementation when a music request reaches this skill.
