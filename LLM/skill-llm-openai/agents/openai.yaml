interface:
  display_name: "OpenAI GPT-5.6 Direct Models"
  short_description: "Wire selected GPT-5.6 and gpt-image-2 direct API routes."
  default_prompt: "Use $skill-llm-openai to build a direct OpenAI integration with an exact profile, full selected model ID, surface/version, request URL, role/history, reasoning/state/cache/tool capability, regional price scope, normalized response, logging, progress, errors, and evidence."

policy:
  allow_implicit_invocation: true
