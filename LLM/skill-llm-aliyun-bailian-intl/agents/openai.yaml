interface:
  display_name: "Aliyun Bailian International Direct Models"
  short_description: "Wire Alibaba Cloud International selected direct-model routes."
  default_prompt: "Use $skill-llm-aliyun-bailian-intl to build an International direct-model integration. Resolve profile, canonical request kind, selected model, exact surface/version, request URL, role/history, billing/deployment/serving scope, capability, transport, progress, errors, and evidence; never use China Mainland routes."

policy:
  allow_implicit_invocation: true
