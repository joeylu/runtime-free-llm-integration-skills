interface:
  display_name: "MiniMax China Mainland Direct Models"
  short_description: "Wire MiniMax China Mainland selected text, image, and music routes."
  default_prompt: "Use $skill-llm-minimax-cn to build a China Mainland integration with exact profile, selected model, surface/version, URL, role/history, capability, price, and transport contracts. Keep plan text-chat only; video-generation is reference-only until a complete selected provider contract is added."

policy:
  allow_implicit_invocation: true
