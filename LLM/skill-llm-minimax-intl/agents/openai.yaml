interface:
  display_name: "MiniMax International Direct Models"
  short_description: "Wire MiniMax International M3 chat/vision plus image-01 and music-3.0 flows."
  default_prompt: "Use $skill-llm-minimax-intl to wire MiniMax International M3 chat/vision or build-profile image/music integration with the exact model/surface parameters, profiles, catalog, request URLs, transport, progress, and error contracts."

policy:
  allow_implicit_invocation: false
