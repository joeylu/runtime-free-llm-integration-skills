interface:
  display_name: "MiniMax International Direct Models"
  short_description: "Wire MiniMax International selected text, image, and music routes."
  default_prompt: "Use $skill-llm-minimax-intl to build an International integration with exact profile, selected model, surface/version, URL, role/history, capability, price, and transport contracts. Keep plan text-chat only; video-generation is reference-only until a complete selected provider contract is added."

policy:
  allow_implicit_invocation: true
