# MiniMax International Vision Transport

No locally selected MiniMax International vision rows exist yet.

Block `RequestKind = vision` until all of these are verified from official MiniMax International docs:

- selectable model row in `model-catalog.md`
- request URL row in `request-urls.md`
- `Supports Image Input = verified` in `capability-matrix.md`
- request and response mapping rules

Use `unsupported_option` for explicit unsupported fields and `capability_unverified` for unknown fields.
