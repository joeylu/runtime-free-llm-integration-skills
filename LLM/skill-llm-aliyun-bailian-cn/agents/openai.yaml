interface:
  display_name: "Aliyun Bailian China Mainland Direct Models"
  short_description: "Wire Aliyun China Mainland selected text, multimodal, and image routes."
  default_prompt: "Use $skill-llm-aliyun-bailian-cn to build a China Mainland direct-model integration. Resolve profile, canonical request kind, selected model, exact surface/version, request URL, role/history, capability, regional price scope, transport, progress, errors, and evidence; never use International routes."

policy:
  allow_implicit_invocation: true
