interface:
  display_name: "DeepSeek Direct Models"
  short_description: "Wire DeepSeek chat, thinking, JSON, streaming, and tool flows."
  default_prompt: "Use $skill-llm-deepseek to wire a DeepSeek API direct-model integration with the right catalog, connection profile, request URLs, transport, thinking, structured output, tool, progress, and error contracts."

policy:
  allow_implicit_invocation: false
